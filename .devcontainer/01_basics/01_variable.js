let name= 'zaid';
var surName='aslam';  // now we don't use var 
const accountId=21213;
let accountState;
acccountCity="jaipur"

// if we change value of variables
name='Zaid aslam'
surName='Khan'
//accountId= 3232;  // can not changed

console.log(accountId);
console.log(acccountCity);
console.log(accountState);
console.table([name,surName,accountId,acccountCity])

